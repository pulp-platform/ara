cache_root_dir: /usr/scratch2/dolent1/gitlabci/buildcache/ara

artifacts:
  tc-gcc:
    inputs:
      - Makefile
      - toolchain/riscv-gnu-toolchain
    outputs:
      - install/riscv-gcc

  tc-verilator:
    inputs:
      - Makefile
      - toolchain/verilator
    outputs:
      - install/verilator
